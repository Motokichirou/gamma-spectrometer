# Gamma Spectrometer — проект

**Детектор:** NaI(Tl) Ø63×63 мм + ФЭУ Hamamatsu R1307  
**MCU/ADC:** STM32G474 (12-bit ADC, 4 Msps)  
**Отвечать пользователю на русском языке.**

> **При любой работе с KiCad — читать в начале сессии: `docs/cheatsheet_mistakes.md` + все остальные `docs/*.md` файлы проекта.**

---

## Структура проекта

```
gamma-spectrometer/
├── docs/
│   ├── adc_mcu_board.md          # Плата МК v1.6 (схемотехника)
│   ├── divider_board.md          # Плата делителя + ADA4817 TIA
│   ├── usb_power_board.md        # USB/питание (общее описание)
│   ├── usb_power_board_schematic.md  # USB/питание (детальная схема)
│   ├── hv_board_schematic.md     # Плата ВВ питания (детальная схема)
│   ├── cheatsheet_mistakes.md    # Шпаргалка: типовые ошибки и правила проверки
│   ├── fab_pselectro.md          # Требования ПС-Электро + сверка (заказ идёт туда)
│   └── refs/                     # Даташиты
├── firmware/                     # STM32 прошивка: этапы 1-2 + самотест + калибровка на Nucleo ✓ (этап 3 — боевая плата)
├── host/                         # ПО на PC `gammapult` (Win32/ImGui, GUI + CLI) — связь/диагностика/калибровка
├── pcb/                          # KiCad: 4 схемы ✓ ERC 0; разводка в процессе (USB+HV разведены, GND-полигоны, межплатная шина; MCU+делитель впереди)
└── simulation/
    ├── models/                   # opamp_g474.sub, opamp_g474_noise.sub, ad8000p.cir
    ├── scripts/                  # analyze_sim1.py, analyze_sim2.py, analyze_sim3.py
    ├── sim1_ad8000.cir           # DONE ✓
    ├── sim2_shaper.cir           # DONE ✓
    └── sim3_noise.cir            # DONE ✓
```

---

## Стек плат (физический порядок снизу вверх)

```
[торец — USB-C кабель]
  1. USB/Power board    Ø45мм  USB-C (JAE DX07S024WJ3R400)
  2. HV board           Ø45мм  MAX1847 flyback + 8-ступ. CW умножитель, 0…−1500В
  3. MCU board          Ø45мм  STM32G474, шейпер CR-RC², ADC
  4. Divider board      Ø42-45мм  Делитель R1307 + ADA4817 TIA
[торец — ФЭУ R1307 + NaI(Tl)]
```

**Межплатные соединения:**
- Stack: 2×N pin header 2.54мм (размер уточняется при разводке)
- Сигнал (делитель→МК): RG-178 коаксиал + U.FL разъёмы
- Питание делителя (МК→делитель): JST PH 3-пин (+5V_A, AGND, −5V_A)
- −ВВ (ВВ board→делитель): экранированный провод ПВВ-1 (≥5кВ), вдоль края стека

---

## Цепь сигнала

```
NaI(Tl)+R1307 → ADA4817 TIA (Zt=499Ω) → коаксиал → CR(OPAMP1) → RC(OPAMP2) → PGA×4(OPAMP3) → ADC
[плата делителя]   ±5V_A                  U.FL      [плата МК — G474]                           3.3V
анод≈0В(вирт.земля) amp_out=+469мВ@Cs137            τ=1мкс       τ=510нс     DC=0.41В
```

**Питание ADA4817 (TIA):** ±5V_A с USB board → через стек → JST PH на плату делителя  
**Полярность:** anode ЗАБИРАЕТ ток ⇒ выход TIA идёт ВВЕРХ ⇒ adc_in идёт **ВВЕРХ** от baseline 0.41В.
В DSP: `amplitude = sample − baseline`. (Исправлено 2026-08-07: прежняя конвенция «вниз» выросла из
`sim2_shaper.cir`, где источник тока анода был развёрнут — см. `sim_polarity_headroom.cir`.)

---

## Симуляции (все DONE ✓)

### Sim1 — AD8000 (DONE ✓, ⚠ ИСТОРИЧЕСКИЙ — вход заменён на ADA4817 TIA)
`simulation/sim1_ad8000.cir` + `scripts/analyze_sim1.py`
- Был AD8000-инвертор: Rfb=1.5к, Cfb=1п → BW=99МГц, peaking<0.5dB ✓
- **Заменён (2026-06-24):** вход = TIA на ADA4817 (Rf=499, Cf=4.7п). Устойчивость/шум — `sim_tia_real.cir`, `sim_tia_stab.cir`, `sim_divider_complete.cir`.

### Sim2 — CR-RC² шейпер (DONE ✓)
`simulation/sim2_shaper.cir` + `scripts/analyze_sim2.py`
- PGA ×4, делитель B Rd=**909Ω** (V_PB0=0.099В, V_out_DC=**0.41В**; было 5.9к/0.529В/2.13В — под ошибочную полярность)
- PF=0.202 (G474 GBW=13МГц; идеальный ОУ в sim = 0.209 — прежняя «0.36» была ошибкой аналитики)

| Источник | Канал/8192 |
|---|---|
| Am-241 (59.5 кэВ) | 105 |
| Cs-137 (662 кэВ) | 1159 |
| Co-60 (1.33 МэВ) | 2321 |
| Tl-208 (2.6 МэВ) | 4554 |
| 3.5 МэВ потолок | 6133 |

### Sim3 — Шум (DONE ✓)
`simulation/sim3_noise.cir` + `scripts/analyze_sim3.py`
- σ_total = 94.4 мкВ rms, σ_baseline = 0.31 канала rms ✓

---

## Платы — статус проектирования

### USB/Power board (СХЕМА В KICAD ✓, ERC ✓)
`docs/usb_power_board_schematic.md` + `pcb/gamma_spectrometer/usb_power_board.kicad_sch`
- LP5907 (U101, +3.3V_A), AMS1117CD-3.3 (U102, +3.3V_D), TPS60400DBV (U104, −5V_A)
- FT231XQ (U103, QFN-20-1EP_4x4mm), PRTR5V0U2X (U_ESD101)
- LC-фильтры 47мкГн на +5V_A и −5V_A (84дБ @ 300кГц)
- **+5V_HV** — power symbol, выход LC-фильтра (L103+C115+C116) от +5V_USB
- **Бюджет USB 2.0**: ~370 мА < 500 мА ✓
- Footprint AMS1117: SOT-223 ✓ (назначен)
- Footprint FT231XQ: QFN-20-1EP_4x4mm ✓ (назначен)
- C112, C113 (−5V шина): полярность обратная (+ к GND)

### HV board (СХЕМА В KICAD ✓)
`docs/hv_board_schematic.md`
- **MAX1847EEE+** (U201) flyback контроллер + IRLML0040 (Q201) + ATB322524 T201 (1:10.2) + 8-ступ. CW умножитель
- Питание: +5V_HV (через LC-фильтр с USB board, сырой USB 5В), C206=47мкФ X7R (GRM32ER71A476KE15L) + C204=0.1мкФ + C207=0.1мкФ
- **Снаббер первичный**: D201(BAS516) Анод→Drain/T201pin4, Катод→C208(10нФ)+D202+D203(2×BZT52C18 серия)→GND
- **Вторичная обмотка**: T201 pin2(dot)→GND, T201 pin3→C209 (прямо в CW, без отдельного выпрямителя)
- **CS цепь**: R205(0.1Ом) + C205(100пФ) параллельно, от Source Q201 к GND
- **RC-фильтр выхода**: C13a201‖C13b201+C14a201‖C14b201 (сглаживание) → R220→R231→R232 (3×100кОм) → C15a/b201, C16a/b201, C17a/b201 (к GND) → R233(10Ом)+C18a201‖C18b201 (ВЧ фильтр) → −HV_out; все ВВ конденсаторы: C1812X104K102T 2шт в серии = 50нФ/2кВ; C18: CC1206JKNPOCBN221 2шт в серии = 110пФ/2кВ
- **Обратная связь FB**: CW_out → R207+R209–R217 (10×100МОм 1206, суммарно 1ГОм) → FB; R208 (2.2МОм) от HV_CONTROL к FB; V_HV = −V_CTRL × 454.5; MAX1847 встроенный усилитель ошибки, MCP6001 не используется
- Мониторинг: R221–R230 (10×6.8МОм) + R219(110кОм) + TL431DBZ (U202, Vref_2.5V) → HV_MONITOR напрямую на MCU ADC PA0; R_th≈110кОм, время выборки ADC ≥247.5 цикла
- HV_ENABLE: активный HIGH (MCU HIGH = HV вкл); R204 (10кОм серия) + R203 (100кОм pull-down к GND) на SHDN MAX1847; без P-MOSFET
- ⚠ Снаббер: 4В запас (Vds 40В − 36В клamp) — достаточно при наличии C208
- ⚠ **Заливка силиконом** (вся плата после сборки и проверки)

### MCU board v1.6 (СХЕМА В KICAD ✓, ERC ✓)
`docs/adc_mcu_board.md`
- STM32G474MET6 (KiCad: U302), внутренние OPAMP1/2/3, ADC1 12-bit 4Msps
- Вход: U.FL J_sig_in (±~470мВ @Cs-137 от ADA4817 TIA на делителе)
- Питание делителя: JST PH J_pwr_div (±5V_A, AGND)
- **Входной усилитель убран с МК** (перенесён на плату делителя в v1.6; там сейчас ADA4817 TIA)
- Шейпер: R_cr_in=10к(R309) + C_diff=100п(C315) + R_diff=10к(R305) [CR], R_int=1к(R308) + C_int=510п(C316) + R_stab=1к(R306) [RC]
- **REF35250QDBVR** (KiCad: U301, SOT-23-6) — VREF+ **2.500В** + делители DC-смещения. Последовательная опора,
  12 ppm/°C, ±0.05 %. Заменила TL431DBZ 2026-08-07 (ревью). `R301` удалён; `C303` = 1 мкФ (обязательный),
  `C322` = 1 мкФ на вывод NR. Нагрузка узла 0.643 мА при допустимых 10 мА
- **UART — КРЕСТ**: PA9 (TX) → метка UART_RX (на FTDI RXD); PA10 (RX) → метка UART_TX (от FTDI TXD)
- W25Q64JVSSIQ (KiCad: U303) — SPI flash
- Кварц ABM8G-16МГц (KiCad: Y301)
- Pt1000 термодатчик (Honeywell 700-102BAA-B00, внешний на корпусе кристалла): R303(10к) pull-up → TSENS_PWR; TSENS_IN → PB12 (ADC4_IN3 с oversampling 256×)
- HV_CONTROL: PA8 PWM → R302+R304 RC-фильтр → метка HV_CONTROL
- ERC: 2 ошибки исправлены (PWR_FLAG на GND удалён; PWR_FLAG добавлен на VDDA после FB301)

### Divider board (СХЕМА В KICAD ✓)
`docs/divider_board.md`
- ФЭУ: **R1307-01** (гибкие выводы, без цоколя), 11 выводов, J_PMT401 = Conn_01x11
- Делитель: R401–R410 + R412 (цепочка, базовые и эмиттерные), C408–C410 (накопительные Dy6–Dy8), Q401/Q402/Q403 MMBTA42 (буферы Dy6/Dy7/Dy8). Резисторов R413–R416 и капов C401–C407 в схеме НЕТ (удалены 2026-06-24). **R411 снова есть, но теперь это R_SNUB 22 Ом на выходе TIA** (2026-09-19, см. ниже)
- ⚠ **Топология буферов — АКТИВНАЯ (Hamamatsu Fig 5-15), ток смещения идёт ЧЕРЕЗ динод.** `R408` (1.3 М) намеренно соединяет резистивную цепочку с динодной линией DY5→DY6 — именно это даёт ~96 мкА через транзисторы. Базовые: R407=1.3М, R409=1.6М, R410=2М, R412=2М. Актуальная редакция — правило #8 в `cheatsheet_mistakes.md`. ⛔ Прежняя формулировка («динод = ТОЛЬКО эмиттер, цепочка динода НЕ касается», 2026-06-11) ОТМЕНЕНА: по ней ток пойдёт мимо транзисторов и буферы уйдут в отсечку
- Низ цепочки: R412 → **GND напрямую**; узлов `mid_P` и `R414` не существует. DC анода ≈ 0 (виртуальная земля TIA)
- **ADA4817-1ARDZ (U402) — TIA**: анод P **напрямую на −IN** (виртуальная земля); R417=Rf(499)‖C415=Cf(4.7пФ NP0) **между FB(пин1) и −IN(пин2)** (пин1 внутри = выход; cheatsheet #11); Rin/Rg убраны; выход (пин6) → **R411 22 Ом (R_SNUB, обязателен — без него TIA генерирует при реальной Cin 3.4–4.6 пФ, `simulation/tia_parasitics/README.md`)** → AMP_OUT; +IN (пин3) → GND виа в паде; PD(8)→+5V_A, EPAD→AGND; C411-C414 децепинг ±5V_A. Zt=499Ω, −18% шума vs AD8000
- ⚠ **НЕ ставить GND-символ на P** — анод идёт ТОЛЬКО на −IN(пин2) U402, где TIA держит виртуальную землю; реальная земля на P закоротит сигнал. (Прежнее обоснование «R415(Rin) — единственный путь тока к GND» отменено: R415/R416 удалены при переходе на TIA)
- Выход сигнала: OUTPUT ADA4817 (пин6) → R411 22 Ом → метка AMP_OUT → пайный коаксиал (COAX2/COAX4) на плату МК
- −ВВ, питание ±5V_A, разъёмы TP1/TP2/J_pwr — при разводке PCB
- Диаметр ~42–45мм
- Реализовано (2026-06-24, валидировано `sim_divider_complete.cir`): делитель R401-R412 (поздняя секция R407=1.3М/R408=1.3М/R409=1.6М/R410=2М/R412=2М), буферы Q401-403 + защитные диоды D401-403 (1N4148W), капы Dy6-Dy8 (C408=22н/C409=47н/C410=0.1мк), вход TIA **U402(ADA4817-1ARDZ)**+R417(499)+C415(4.7п)+C411-C414, J_PMT401. Метка AMP_OUT на OUTPUT.
- Разъёмы (J_sig, J_pwr, TP1, TP2) — добавляются при разводке PCB, не в схеме

---

## Что осталось

1. **Полный BOM с MPN/footprint** — ✅ ВЫПОЛНЕНО (сессия 5; пересчёт 2026-06-24 после пересборки делителя): см. `bom_fp_work.md`. Актуальный счёт по `pcb/gamma_spectrometer.net` (**177 компонентов**): **74 конденсатора** / 17 MPN (прежние «81» — до удаления bypass C401-C407 на делителе), **63 резистора** (61 нумер. + 2 R_CC; прежние «67» — до удаления R411/R413/R415/R416 при переходе на TIA). ⚠ 2026-09-19: −C320 (плата МК), +R411 22 Ом (делитель) — `.net` перевыгрузить и пересчитать, индуктивности/LED/феррит/предохранитель.
2. **Разводка PCB** — 🚧 В ПРОЦЕССЕ. Все 4 платы в одном `gamma_spectrometer.kicad_pcb`, KiCad 9, **2 слоя** (`F.Cu / core FR4 1.51 / B.Cu` = 1.60 мм; переведено с 4 слоёв 2026-08-07 под ЛУТ-прототипы — на In1/In2 разводки не было, связность не изменилась). Контуры Ø45: USB (100,100), ВВ (170,100), МК (240,100), делитель (310,100). **USB/Power** и **HV**: разведены, GND-полигоны на F/B. **Межплатная шина**: цанг-пины `INTERCONNECT_PIN_D1.5` на дуге R20 — 11 на USB, 14 на ВВ и МК (у USB нет трёх ВВ-цепей), углы 1:1 → соосны; стойки `STANDOFF_M2.5_GND_x3_TRIANGLE` в центре всех 4 плат, пады на GND. `J_PMT401` стоит на делителе (поворот −69°, подобран по ВВ-зазорам). Всем механическим футпринтам проставлен `board_only`. ⚠ Кросс-платные «не подключено» в DRC — норма (мост = цанг-пины в стеке); межплатные цепи скрыты через `hidden_nets` в `.kicad_prl`. Все 4 платы разведены. `J101` переведён на проектную копию футпринта — экран разъёма заземлён. Межплатные соединения: `−HV` идёт мимо МК через сквозное отверстие Ø5 (`H_HV301`), `AMP_OUT` — пайным коаксиалом через пары `COAX_PIN_D0.5` (сигнал+оплётка), Pt1000 — на пятаки `TP_TSENS301`.
**Нормы ВВ-зазоров** (3 мм/кВ) заданы в `pcb/gamma_spectrometer/gamma_spectrometer.kicad_dru` — 49 правил.
⚠ **ВПЕРЕДИ**: перезалить зоны в Pcbnew и развести **59 мест**, где земля подходит к ВВ-цепям ближе нормы; шелкография (≈400 нарушений) перед заказом. Детали и грабли — `docs/session_context.md`.
3. **STM32 прошивка** — ✅ этапы 1-2 на Nucleo (DMA ADC, пик-детектор, гистограмма, shproto) + самотест ENC + порог DSP + энергокалибровка во flash (совместима с кнопками BecqMoni). Осталось: этап 3 на боевой плате, термокомпенсация Pt1000.
4. **ПО на PC `gammapult`** — 🚧 GUI + CLI (Win32/ImGui, единый .exe): связь, диагностика, живой спектр, мастер энергокалибровки. Подробности — `host/README.md`. Протокол калибровки BecqMoni разгадан — `docs/ru/protocol.md`.
5. 🔴 **БЛОКЕРЫ ЗАКАЗА (найдено 2026-08-08 прогоном скиллов kicad-happy):**
   - ✅ **SWD — РЕШЕНО проводами (2026-09-19, решение пользователя).** Пятаков SWD на плате НЕ будет:
     на время прошивки/обслуживания тонкие провода паяются прямо к выводам U302 — **SWDIO = вывод 63
     (PA13), SWCLK = 64 (PA14), NRST = 7 (PG10)**; GND и +3.3 В — с падов ближайших конденсаторов развязки.
     → **`TP_SWD301` удалить с платы** (футпринт `board_only`, в схеме его нет) — иначе 4 нитки в DRC.
     Провода после прошивки лучше снимать (антенна на выводы МК рядом с −ВВ и шейпером); если остаются —
     100 нФ NRST→GND.
     **Обновления без проводов (план, в прошивке НЕ сделано):** команда протокола → программный переход
     в системный загрузчик → STM32CubeProgrammer по COM-порту прибора. Проверено по `docs/refs/an2606.pdf`
     (Rev 61, §47, Табл. 101): загрузчик G47x работает по **USART1 PA10=RX / PA9=TX** (наши выводы к FT231X),
     8 бит, even, 1 стоп. ⚠ Перед переходом прошивка ОБЯЗАНА выключить ВВ (HV_ENABLE=0): загрузчик
     настраивает **PA8 как I2C2_SDA с подтяжкой вверх** — это наш ШИМ HV_CONTROL, фильтр уйдёт к 3.3 В
     (≈ −1470 В при включённом ВВ). Загрузчик слушает и USART2 на **PA2/PA3** (выводы OPAMP1) — при
     выключенном ВВ там статический уровень, ложного выбора интерфейса не ожидается. Провода нужны
     только для первой прошивки и восстановления после неудачного обновления.
   - **BOOT0 (PB8) висит в воздухе** — `unconnected-(U302-PB8-Pad77)`. По `DS12288` вывод 77
     LQFP80 = PB8-BOOT0; по `RM0440` заводское `FLASH_OPTR = 0xFFEFF8AA`, бит 26 `nSWBOOT0 = 1`
     ⇒ BOOT0 читается **с вывода**, а он не подключён и на сбросе висит входом.
     Режим загрузки не определён. Два пути (оба рабочие):
     1. 10 кОм PB8→GND в схеме — надёжно при любых option bytes;
     2. **без правки платы:** при первой прошивке по SWD записать option bytes
        `STM32_Programmer_CLI -c port=SWD -ob nSWBOOT0=0 nBOOT0=1` ⇒ вывод BOOT0 игнорируется,
        загрузка всегда из основной флеш (`RM0440` §2.6.1, Табл. 5: nSWBOOT0=0 & nBOOT0=1 → Main flash).
        SWD доступен и если чип стартовал в загрузчик — первая прошивка возможна при любом BOOT0.
     ⚠ В доке §5 PB8 числится как `LED_HV` — это и есть BOOT0, светодиод туда вешать нельзя.
   - **Пояски виа 0.15 мм** при норме ПС-Электро ≥0.25 (155 виа: пад Ø0.6 / сверло Ø0.3)
     → пад Ø0.8. **Шелкография 0.8–1.2 мм** при норме ≥1.3 (234 надписи, т.е. все).
     **Сверло Ø2.75** ×12 вне номинального ряда; **Ø5.0** — только фрезеровкой. `docs/fab_pselectro.md`.
   - **MPN лежат в поле `PN`, а не `MPN`** (172/177 компонентов) — ни один BOM/CPL-экспорт их не увидит.
6. ⚠ **К проверке перед заказом:** D202/D203 footprint должен быть `Diode_SMD:D_SOD-123` (BZT52C18 Diotec в стандартном SOD-123, не -123W). Футпринты межплатки/ФЭУ сделаны (сессия 14, 2026-06-16) в библиотеке `посадочные места`: `INTERCONNECT_PIN_D1.5`, `STANDOFF_M2.5_GND`(+`_x3_TRIANGLE` R19), `J_PMT401_R1307-01` (реальный обмер: 18 слотов, круг уменьшен Ø35→Ø30 под стойки). Проверить линейкой в редакторе перед заказом.
7. **v2 (будущий апгрейд):** MCU board v2.0 на AD4084 (16-bit 20 MSPS DPP) — ТЗ в `docs/ad4084_dpp_design.md`. ⛔ ТЗ устарело (стоит на AD8000 + импульсах «вниз» + 4 слоях), см. врезку в начале файла. Решение 2026-06-10: сначала v1 в железо. v2 = замена только MCU board в стеке.
8. **✅ Делитель пересобран целиком (2026-06-24)** — активный делитель приведён к Hamamatsu Fig 5-15 + вход на TIA. Подтверждено единой моделью `simulation/sim_divider_complete.cir`:
   - **Буферы Q401-403 смещены ~96 мкА** (был изъян: ток шёл мимо транзисторов, отсечка — исправлено: цепочка через диноды, эмиттерные R; поздняя секция R407=1.3М/R408=1.3М/R409=1.6М/R410=2М/R412=2М).
   - **Защитные диоды B-E D401-403** (1N4148W SOD-123, анод→эмиттер, катод→база; V_EBO=6В защита на рампе −HV).
   - **Вход — TIA на ADA4817-1ARDZ (U402)** вместо AD8000: Rf=499, Cf=4.7пФ, анод напрямую на −IN; Rin/Rg убраны. −18% шума (76.9 мкВ), Zt=499Ω, 3.5 МэВ→2.47В без клиппинга. Капы bypass только Dy6-Dy8.
   - Осталось по делителю: разводка PCB.

---

## LTspice запуск
```powershell
& "C:\Program Files\ADI\LTspice\LTspice.exe" -b "C:\gamma-spectrometer\simulation\sim2_shaper.cir"
python simulation/scripts/analyze_sim2.py simulation/sim2_shaper.raw
```

## Ключевые детали

- **opamp_g474.sub**: XU1 — `XU1 INM INP_off OUT opamp` (pin1=IN−, pin2=IN+)
- **.raw формат**: transient — time=8B double, vars=4B float; noise — все 4B float
- **AMS1117**: нужен танталовый конденсатор на выходе, с керамикой нестабилен
- **TPS60400 C110(VIN)/C111(CFLY)**: X7R обязательно, не Y5V (bypass VIN и летающий конденсатор)
- **MAX1847 POL**: подключить к VL (N-channel), VL bypass 0.47мкФ
- **MAX1847 COMP**: R201 (1МОм) + C201 (33нФ) последовательно к GND
- **MAX1847 FREQ**: только R202 к GND; R202=180кОм → f≈252кГц
- **HV_ENABLE**: активный HIGH (MCU HIGH = HV вкл); R204 (10кОм) серия + R203 (100кОм) pull-down к GND на SHDN; P-MOSFET не используется
- **+5V_HV**: шина сырого USB 5В через LC-фильтр (L103+C115+C116 на USB board) → HV board; не проходит на MCU board
